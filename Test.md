# Claude Code斜杠命令保姆级企业级全教程（入门到大神·官方正版命令）

说明：本教程仅收录官网原生真实命令，无虚构、无杜撰，严格依据官方50+内置斜杠命令拆解，采用企业级工程视角，以「专业定义+大白话解读+实战场景+可复制示例+独家避坑注意」标准编写，配套完整大厂项目落地流程，零基础可直接上手，适配团队标准化开发、面试进阶、项目提效。

## 一、Claude Code 斜杠命令全分类精讲（官方原生全覆盖）

Claude Code 所有命令输入 / 即可唤起下拉菜单，区别于普通聊天对话，斜杠命令是强制标准化工程指令，可以精准控制会话、上下文、代码读写、并行任务、子代理、代码审查、项目记忆等核心能力，是企业AI开发效率翻倍的核心。

### 1.1 基础会话管理命令（日常开发高频必备）

#### 1、/help 帮助查询

核心作用：展示当前客户端所有原生斜杠命令、使用说明、快捷键、权限适配规则，是官方最全指令清单。 实战场景：忘记命令用法、版本更新核对新增指令、新人快速查手册。 使用示例：直接在终端会话输入 /help 回车即可。 注意事项：不同客户端版本命令数量略有差异，一切以本地 /help 展示结果为准，杜绝网上过时文档误导。

#### 2、/clear 清空会话

核心作用：清空当前会话所有对话上下文，重置全新空白会话，彻底隔绝历史需求干扰。 实战场景：完成一个业务模块开发，切换全新需求、重构、Bug排查场景，避免旧上下文跑偏。 使用示例：开发完登录模块，切换支付模块开发，输入 /clear 重置会话。 注意事项：仅清空当前展示对话，本地历史会话缓存不会删除，可通过恢复命令找回数据，不会造成开发记录丢失。

#### 3、/resume 恢复会话

核心作用：读取本地缓存的历史会话记录，一键恢复上次中断的开发进度。 实战场景：关机、关闭终端、中途暂停开发，二次接续迭代任务。 使用示例：隔天继续未完成的项目重构，输入 /resume 恢复历史会话。 注意事项：仅恢复本机本地会话，跨设备无法同步；重要项目建议搭配 /export 手动归档。

#### 4、/rewind 会话&代码回滚

核心作用：双向回滚能力，既可以撤回对话步骤，也能撤销 Claude 自动修改的本地代码文件，类似轻量 Git 版本回退。 实战场景：AI重构代码出错、逻辑改错、批量生成代码不符合预期，一键回撤到变更前状态。 使用示例：重构工具类后项目报错，输入 /rewind 选择对应变更节点回退。 注意事项：仅撤回 Claude 自动操作的代码，开发者手动修改的内容不会被改动；支持多节点批量回滚。

#### 5、/btw 旁路临时提问

核心作用：临时穿插小额问答，提问内容不写入主线上下文，不占用 Token、不污染核心开发需求。 实战场景：开发中临时查询语法、配置、报错含义，不想打乱当前编码节奏。 使用示例：/btw Vue3 组合式API生命周期执行顺序 注意事项：btw 问答不会存入会话摘要，核心业务知识、重要方案禁止使用该命令提问。

#### 6、/export 会话导出

核心作用：将当前完整对话、代码变更、方案记录一键导出为本地文档，用于归档复盘。 实战场景：迭代总结、线上故障复盘、需求方案留存、新人教学归档。 使用示例：/export 订单模块重构迭代复盘文档.md 注意事项：导出文件默认存于项目根目录，支持 md、txt 格式，建议企业项目迭代必导出归档。 核心作用：自动梳理当前会话所有工作内容，精简输出「已完成+待完成+核心方案」摘要。 实战场景：多人协作交接、长时间开发后复盘、迭代进度汇总汇报。 使用示例：多轮开发结束后，输入 /recap 生成精简工作总结。 注意事项：摘要仅梳理主线内容，btw 旁路提问内容不会纳入汇总。

### 1.2 项目记忆持久化命令（团队协作核心）

该系列命令是企业团队标准化的核心，实现「一次配置、永久生效、跨会话复用」，解决每次对话重复解释项目规则的痛点。

#### 1、/init 项目初始化

核心作用：自动扫描项目技术栈、目录结构、依赖配置，一键生成 CLAUDE.md 项目记忆文件。 实战场景：新项目接入 Claude Code、老项目首次标准化适配。 使用示例：项目根目录进入会话，直接输入 /init 按提示完成初始化。 注意事项：企业团队必须将生成的 CLAUDE.md 提交 Git，全员共用统一规范，禁止本地私有配置。

#### 2、/memory 编辑持久记忆

核心作用：在线可视化编辑 CLAUDE.md，实时更新项目技术规范、编码规则、禁用依赖、目录标准，跨会话永久生效。 实战场景：项目技术栈升级、编码规范迭代、新增强制开发规则。 使用示例：输入 /memory 打开编辑面板，补充统一返回格式、代码行数限制规则。 注意事项：仅写入长期不变的硬性规范，禁止写入临时Bug、调试日志、单次业务需求。

#### 3、/goal 锁定会话目标

核心作用：强制锁定当前会话核心任务，约束模型不发散、不跑偏，所有操作围绕设定目标执行。 实战场景：大型迭代、多轮开发、复杂架构设计，防止AI自由发挥偏离需求。 使用示例：/goal 完成用户权限模块开发，实现角色分配、权限校验、接口拦截、日志记录 注意事项：单次会话仅保留一个核心 goal，切换需求需重新设置。

### 1.3 上下文与 Token 管控命令（企业降本+防失忆核心）

#### 1、/context 上下文检测

核心作用：可视化展示当前会话 Token 占用比例、上下文负载情况，直观判断是否需要压缩。 实战场景：长会话开发、多轮迭代后检测模型失忆风险。 使用示例：持续开发1小时后，输入 /context 查看负载。 注意事项：负载超过70%必须压缩，否则会出现需求遗忘、代码错乱、输出截断问题。

#### 2、/compact 上下文压缩

核心作用：智能精简冗余对话、调试过程、重复内容，保留核心方案与需求，大幅释放上下文窗口。 实战场景：长会话卡顿、模型失忆、Token 占用过高。 使用示例：/compact 保留架构设计和最终代码方案，精简所有调试对话 注意事项：压缩不会删除核心成果，仅清理冗余内容，是长会话开发的必用命令。

#### 3、/cost /usage 成本监控

核心作用：/cost 查看当前会话 Token 消耗与预估费用；/usage 查看账号月度额度、剩余可用资源。 实战场景：团队批量管控AI开发成本、避免超额消耗。 注意事项：复杂重构、全量评审 Token 消耗最高，建议高频监控。

### 1.4 代码工程开发核心命令（业务编码主力）

#### 1、/map 项目架构扫描

核心作用：一键生成项目完整目录树、模块依赖关系、文件层级结构，快速摸清项目架构。 实战场景：接手陌生老项目、梳理项目架构、核对目录规范。 使用示例：进入新项目根目录，输入 /map 生成架构图谱。

#### 2、/search 全局代码检索

核心作用：全局检索项目关键字、方法、类、常量、报错信息，精准定位代码位置。 使用示例：/search 令牌过期校验

#### 3、/analyze 代码架构分析

核心作用：分析代码耦合度、冗余逻辑、架构缺陷、性能隐患、设计不合理点。 使用示例：/analyze @src/service/user 分析用户业务代码冗余与耦合问题

#### 4、/refactor 批量代码重构

核心作用：批量优化代码格式、命名、逻辑、解耦、精简冗余代码，统一项目编码风格。 使用示例：/refactor @src/order 去除冗余判断，统一异常处理，规范方法命名 注意事项：重构前必须提交本地代码，防止代码丢失。

#### 5、/docs 自动文档生成

核心作用：一键生成接口文档、README、代码注释、业务说明文档。 使用示例：/docs 基于当前Controller生成完整接口文档，包含参数、错误码、返回示例

#### 6、/sandbox 沙箱执行

核心作用：开启隔离沙箱环境，安全执行打包、编译、脚本命令，避免高危操作影响本地环境。 注意事项：企业生产环境所有脚本执行必须开启沙箱，杜绝直接运行高危指令。

### 1.5 代码审查与质量保障命令（官方核心重点）

该模块为官网重点讲解能力，包含独家 /simplify 简化代码命令，是企业代码提效、提质的关键。

#### 1、/review 代码评审

核心作用：企业级全量代码评审，分级输出严重、高危、中低风险问题，附带精准修复方案。 实战场景：迭代收尾、PR提交前、上线前代码质检。

#### 2、/simplify 代码精简优化（官网独有核心命令）

核心作用：在不改变代码业务逻辑的前提下，精简冗余代码、合并重复逻辑、优化代码结构、降低圈复杂度，让代码更简洁、易读、易维护。 实战场景：代码臃肿、嵌套过多、重复代码多、逻辑冗余的业务文件优化。 使用示例：/simplify @src/service/order 精简代码嵌套与重复逻辑，保留全部业务功能 注意事项：仅做结构精简，不会改动业务逻辑，是重构轻量化最优命令。

#### 3、/bug 漏洞扫描

核心作用：自动扫描语法错误、逻辑Bug、安全漏洞、性能隐患，输出问题清单+修复方案。

#### 4、/unit-test 单元测试生成

核心作用：自动生成全覆盖单元测试用例，包含正常场景、异常场景、边界值场景。

#### 5、/fix 一键修复报错

核心作用：根据报错日志、堆栈信息、代码异常，自动定位问题并修复代码。

### 1.6 并行任务 & 子代理高级命令（官网高阶核心）

Claude Code 支持多任务并行+子代理拆分，是区别于普通AI对话的高阶能力，适合大型项目多模块同时开发。

#### 1、并行协作能力

核心原理：支持同时下发多个独立开发任务，AI自动拆分线程并行处理，无需逐个排队执行，大幅提升多模块开发效率。 实战场景：同时开发控制器、服务层、工具类、文档生成多任务。 注意事项：并行任务过多会轻微增加Token消耗，企业开发建议单次并行不超过5个核心任务。

#### 2、子代理拆分能力

核心作用：自动将复杂大型需求，拆分为多个独立子代理任务，分模块独立执行、互不干扰，最终统一汇总结果。 实战场景：项目架构搭建、全模块重构、批量代码优化。 注意事项：复杂需求优先使用子代理拆分，避免单任务逻辑臃肿、输出质量下降。

### 1.7 权限、配置与调试命令（企业安全管控）

#### 1、/permissions 权限管控

核心作用：精细化管控文件读写、文件删除、网络访问、脚本执行权限，杜绝删库、密钥泄露、高危操作风险。

#### 2、/model 模型切换

核心作用：快速切换推理模型，复杂架构用Opus、日常开发用Sonnet、轻量查询用Haiku，兼顾精度与速度。

#### 3、/doctor 环境自检

核心作用：一键检测安装环境、权限、网络、鉴权状态、依赖问题，自动修复大部分环境异常。

#### 4、/debug 调试日志

核心作用：开启/关闭详细执行日志，排查命令执行失败、脚本报错、模型异常问题。 ---

## 二、企业级全套落地流程（官方命令串联实战）

本流程全部使用官网原生命令，无虚构指令，可直接用于公司项目标准化开发，适配90%前后端企业项目迭代场景。

### 2.1 新项目标准化搭建全流程

``` plaintext
# 1. 进入项目目录启动会话
cd 项目根目录
claude

# 2. 项目初始化，生成团队统一规范
/init

# 3. 锁定本次开发核心目标
/goal 完成项目基础架构搭建，实现核心模块CRUD与基础校验能力

# 4. 扫描项目架构，规范目录
/map

# 5. 业务代码开发+精简优化
/simplify 精简生成的初始代码，优化嵌套逻辑

# 6. 全局漏洞检测
/bug

# 7. 生成单元测试
/unit-test

# 8. 企业级代码评审
/review

# 9. 自动生成项目文档
/docs

# 10. 压缩上下文+迭代归档
/compact 保留架构与核心代码方案
/export 新项目搭建全流程记录.md
```

### 2.2 老项目优化&Bug修复流程

``` shell
claude
/clear                  # 清空无关上下文
/bug                    # 全局扫描代码漏洞与逻辑Bug
/analyze @目标模块       # 分析问题代码根因
/simplify @目标文件      # 精简臃肿代码，优化结构
/refactor @目标路径      # 重构冗余逻辑
/rewind                 # 异常则一键回退变更
/review                 # 二次评审修复效果
/compact 保留Bug根因与修复方案
/export Bug修复复盘报告.md
```

### 2.3 上线前企业标准化质检流程

```Plain Text
/goal 上线前全量质检，阻断所有高危漏洞与不规范代码
/permissions            # 校验安全权限配置
/map                    # 核对项目目录规范
/simplify 统一精简所有新增业务代码
/review                 # 全量代码评审
/unit-test              # 校验测试用例全覆盖
/docs                   # 更新最新接口文档
/cost                   # 管控迭代Token成本
/export 上线验收质检报告.md
```

## 三、高阶提效技巧+避坑总结（大厂实战经验）

### 3.1 命令搭配最优组合

- 代码臃肿优化：/simplify + /refactor 轻量化精简+深度重构组合，效率最高

- 长会话保稳：/context 监控 + /compact 压缩 杜绝模型失忆

- 迭代收尾标配：/review + /bug + /docs + /export 标准化闭环

- 复杂需求标配：子代理拆分 + 并行任务执行，提升3倍开发效率

### 3.2 高频避坑总结

- 禁止滥用 /clear 频繁重置会话，有效会话优先使用 /compact 压缩

- /simplify 只做精简不改逻辑，核心业务重构需搭配 /refactor

- 并行任务不盲目多开，避免Token消耗过高、输出精度下降

- 所有企业项目必须执行 /init 初始化，保证团队规范统一

## 四、官方原生命令速查表（可直接打印）

会话管理：/help /clear /resume /rewind /btw /recap /export 项目记忆：/init /memory /goal 工程开发：/map /search /analyze /refactor /docs /sandbox 质量评审：/bug /review /simplify /unit-test /fix 成本管控：/context /compact /cost /usage 安全配置：/permissions /model /doctor /debug

### 文件下载说明

全文为官网真实原生命令教程，无杜撰内容，可直接全选复制，保存为 .md 或 .txt 文件，支持本地查看、打印、团队知识库上传。

> （注：部分内容可能由 AI 生成）

# 3

123

123

## 123

## 123

### 123
